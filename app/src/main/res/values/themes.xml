<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Plain no-action-bar theme; all real UI is drawn by Jetpack Compose. -->
    <style name="Theme.AiChat" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Shown for a brief moment by the OS before MainActivity draws its first Compose frame. -->
    <style name="Theme.AiChat.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_background</item>
        <item name="postSplashScreenTheme">@style/Theme.AiChat</item>
    </style>

</resources>
